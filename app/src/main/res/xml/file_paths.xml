<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- caché interna -->
    <cache-path name="images" path="images/"/>
    <!-- caché externa (por si el OEM lo requiere) -->
    <external-cache-path name="external_images" path="images/"/>
</paths>

